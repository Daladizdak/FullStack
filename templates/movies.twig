<!doctype html>
	<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
	</head>

	<body>
		<div class="container">
			<h1 class="text-center">List of ALL my movies!!!</h1>

			<table class="table">
				<thead>
					<tr>
						<th>Movie Name</th>
						<th>Genre</th>
						<th>Price</th>
						<th>Date for release</th>
					</tr>
				</thead>

				<tbody>
					{% for movie in movies %}
						<tr>
							<td>{{ movie.Movie_name }}</td>
							<td>{{ movie.Genre }}</td>
							<td>£{{ movie.Price|number_format(2, '.', ',') }}</td>
							<td>{{ movie['Date for release'] }}</td>
						</tr>
					{% endfor %}
				</tbody>
			</table>
	
		</div>
	</body>
</html>